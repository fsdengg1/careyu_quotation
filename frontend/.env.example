# Development-only notes
# Production never uses VITE_* API URLs. The app calls same-origin `/api`.
# Vite already proxies `/api` to http://localhost:4001 during `npm run dev`.
# VITE_API_URL=http://localhost:4001
